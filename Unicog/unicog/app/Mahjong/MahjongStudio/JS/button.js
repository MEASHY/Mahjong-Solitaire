/** Class representing a user interface button*/
class Button {
    /**
     * Create a TileNode
     * @param {context} state      - The scene in which the button resides.
     * @param {number} x           - The x position of the button on screen.
     * @param {number} y           - The y position of the button on screen.
     */
	constructor(state, x, y){
		var session new GameSession()
		this.state = state
		this.x = x
		this.y = y
		this.button = null
	}
}